# LiteMCP Docker Environment Configuration
# Copy this file to .env and modify the values according to your environment
# cp .env.example .env

# =============================================================================
# Backend Service Configuration (后端服务配置)
# =============================================================================

# Proxy Server Configuration (代理服务器配置)
PROXY_HOST=0.0.0.0
PROXY_PORT=1888

# API Server Configuration (API服务器配置)
API_HOST=0.0.0.0
API_PORT=9000

# =============================================================================
# Frontend Service Configuration (前端服务配置)
# =============================================================================

# Frontend Web Server Port (前端Web服务器端口)
FRONTEND_PORT=2345

# API Base URL for Frontend (前端API基础地址)
VITE_API_BASE_URL=http://localhost:9000

# Proxy Base URL for Frontend (前端代理基础地址)
VITE_PROXY_BASE_URL=http://localhost:1888

# Debug Mode (调试模式)
VITE_DEBUG_MODE=false

# Log Level (日志级别)
VITE_LOG_LEVEL=error

# =============================================================================
# Production Environment Example (生产环境示例)
# =============================================================================
# Uncomment and modify the following lines for production deployment
# (取消注释并修改以下行用于生产环境部署)

# PROXY_HOST=0.0.0.0
# PROXY_PORT=1888
# API_HOST=0.0.0.0
# API_PORT=9000
# FRONTEND_PORT=80
# VITE_API_BASE_URL=https://api.yourdomain.com
# VITE_PROXY_BASE_URL=https://proxy.yourdomain.com
# VITE_DEBUG_MODE=false
# VITE_LOG_LEVEL=error

# =============================================================================
# Development Environment Example (开发环境示例)
# =============================================================================
# Uncomment and modify the following lines for development
# (取消注释并修改以下行用于开发环境)

# PROXY_HOST=0.0.0.0
# PROXY_PORT=1888
# API_HOST=0.0.0.0
# API_PORT=9000
# FRONTEND_PORT=3000
# VITE_API_BASE_URL=http://localhost:9000
# VITE_PROXY_BASE_URL=http://localhost:1888
# VITE_DEBUG_MODE=true
# VITE_LOG_LEVEL=debug

# =============================================================================
# Usage Instructions (使用说明)
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Modify the values according to your environment
# 3. Run Docker Compose: docker-compose --env-file .env up
# 4. Or use the deploy script: ./deploy.sh up
#
# 1. 复制此文件: cp .env.example .env
# 2. 根据您的环境修改值
# 3. 运行Docker Compose: docker-compose --env-file .env up
# 4. 或使用部署脚本: ./deploy.sh up
