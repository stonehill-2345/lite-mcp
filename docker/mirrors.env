# Python Package Mirror Configuration
# Choose your preferred mirror source by uncommenting the corresponding section

# =============================================================================
# China Mirrors (中国镜像源) - Recommended for users in China
# =============================================================================

# Alibaba Cloud (阿里云) - Default, fast and stable with CDN
UV_INDEX_URL=https://mirrors.aliyun.com/pypi/simple/
UV_EXTRA_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
UV_HTTP_TIMEOUT=120
UV_CONCURRENT_DOWNLOADS=10

# Tsinghua University (清华大学) - Alternative option
# UV_INDEX_URL=https://pypi.tuna.tsinghua.edu.cn/simple
# UV_EXTRA_INDEX_URL=https://mirrors.aliyun.com/pypi/simple/
# UV_HTTP_TIMEOUT=120
# UV_CONCURRENT_DOWNLOADS=10

# Tencent Cloud (腾讯云) - Alternative option
# UV_INDEX_URL=https://mirrors.cloud.tencent.com/pypi/simple
# UV_EXTRA_INDEX_URL=https://pypi.org/simple

# Douban (豆瓣) - Alternative option
# UV_INDEX_URL=https://pypi.douban.com/simple
# UV_EXTRA_INDEX_URL=https://pypi.org/simple

# =============================================================================
# Global Mirrors (全球镜像源) - For users outside China
# =============================================================================

# Official PyPI (官方源) - Recommended for international users
# Note: The Dockerfile will automatically fallback to this if China mirrors fail
# UV_INDEX_URL=https://pypi.org/simple
# UV_EXTRA_INDEX_URL=
# UV_HTTP_TIMEOUT=120
# UV_CONCURRENT_DOWNLOADS=10

# =============================================================================
# Usage Instructions (使用说明)
# =============================================================================
# 1. Default configuration uses Aliyun mirror (fastest in China)
# 2. To use a specific mirror, uncomment the corresponding section above
# 3. Apply with Docker: docker-compose --env-file docker/mirrors.env up
# 4. Or source in shell: source docker/mirrors.env && ./docker/deploy.sh up
#
# Automatic Fallback Strategy:
#   1st: Aliyun (mirrors.aliyun.com/pypi/simple/)
#   2nd: Tsinghua (pypi.tuna.tsinghua.edu.cn/simple)
#   3rd: Official PyPI (pypi.org/simple)
#   4th: pip install fallback
#
# This ensures the project works globally without manual configuration.
#
# For international users:
# - System auto-falls back to official PyPI if China mirrors fail
# - Or explicitly set: UV_INDEX_URL=https://pypi.org/simple
